export interface MenuSection {
    icon: string; // Path to the icon image
    label: string; // Text label for the menu item
    hasDropdown?: boolean; // Optional: Indicates if the item has a dropdown
  }
  